set timeout=5
set default=0

# Modo texto puro - evita "no suitable video mode found"
insmod all_video
terminal_output console

menuentry "LiwusOS x86_64 (modo texto)" {
	set gfxpayload=text
	multiboot2 /boot/kernel.bin
	module2 /boot/initrd.tar
}